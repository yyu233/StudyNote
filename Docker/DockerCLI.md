```
docker diff CONTAINER  # compare container difference after launching

docker network ls # Lists all the networks the Engine daemon knows about

docker attach # reattach to a detached container
```
