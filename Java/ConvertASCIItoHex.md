Method 1
```
    Convert ASCII string to character array.
    Convert each character to integer.
    Convert integer value to Hex using toHexString().

```

Method 2
```
    use String.format() method to convert ASCII to Hex. Following are the steps -
    Convert ASCII string to character array.
    Use %H to format each character to Hex.

```
